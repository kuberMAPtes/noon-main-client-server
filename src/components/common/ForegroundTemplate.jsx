import React from 'react';
import '../../assets/css/ForegroundTemplate.css'

const ForegroundTemplate = ({ children }) => (
  <div className="foreground-wrapper">
    {children}
  </div>
);

export default ForegroundTemplate;
